<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/green_bad1" />
    <corners android:radius="20dp" />
    <stroke
        android:color="@color/red"
        android:width="2dp" />
</shape>
